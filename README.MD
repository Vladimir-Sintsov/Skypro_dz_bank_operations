# **Проект "банковские операции" это виджет для обработки данных банковских карт и счетов без интерфейса на Python**

## **Функционал реализованный в проекте:**

1. получение информации о транзакциях из JSON, CSV, XLSX-файла
2. маскировка номера счета
3. маскировка номера карты
4. сортировка по дате и возрастанию, убыванию
5. фильтрация по полученным данным




## **Требования к проекту**

1. Python 3.12.7
2. flake8 7.2.0
3. mypy 1.16.0
4. black 25.1.0
5. isort 6.0.1


## Инструкция по установке
1. Чтобы скачать репозиторий:
```
git clone git@github.com:Vladimir-Sintsov/Skypro_dz_bank_operations.git
```

1. Установить не обходимые зависимости
```
pip install -r requirements.txt
```
## Описание работы реализованных функций
```
get_mask_card_number(card_number: str) -> str
```
Маскирует номер банковской карты в формате XXXX XX** **** XXXX.
* Вовод: 7000792289606361
* Вывод: 7000 79** **** 6361

```
get_mask_account(account_number: str) -> str
```
Маскирует номер банковского счёта, отображая только последние 4 цифры.
* Ввод: 73654108430135874305
* Вывод: **4305

```
mask_account_card(input_string: str) -> str
```
Автоматически определяет, что маскировать — карту или счёт — и возвращает результат с сохранением типа.
* Ввод: "Visa Platinum 7000792289606361"
* Вывод: "Visa Platinum 7000 79** **** 6361"

* Ввод: "Счет 73654108430135874305"
* Вывод: "Счет **4305"

```
get_date(iso_string: str) -> str
```
Преобразует дату из формата ISO 8601 в формат ДД.ММ.ГГГГ.
* Ввод: "2024-03-11T02:26:18.671407"
* Вывод: "11.03.2024"

```
filter_by_state(operation_list, state='EXECUTED')
```
Фильтрует список операций, возвращая только те, у которых ключ `state` равен заданному значению.

- **operation_list** — список словарей, где каждый словарь описывает операцию.
- **state** — строка, статус операции для фильтрации (по умолчанию `'EXECUTED'`).

Возвращает: список операций с указанным статусом.

```
sort_by_date(operation_list)
```
Сортирует список операций по ключу `date` в порядке от самой новой к самой старой.

- **operation_list** — список словарей операций с ключом `date` в формате ISO.

Возвращает: отсортированный список операций.


### примеры использования 
``` 
from src.widget import get_date
from src.widget import mask_account_card

print(mask_account_card("Maestro 1596837868705199"))
print(mask_account_card("Счет 64686473678894779589"))
print(get_date("2024-03-11T02:26:18.671407"))
```



```
get_searched_transactions
``` 
Принимает список словарей с данными о банковских операциях и строку поиска,
а возвращает список словарей, у которых в описании есть данная строка. ,

```
get_transactions_count_by_category
``` 
Принимает список словарей с данными о банковских операциях и список категорий операций,
а возвращает словарь, где ключи — это названия категорий, а значения — это количество операций в каждой категории.


# Модуль `generators` предоставляет удобные генераторы и функции для работы с транзакциями и генерацией номеров банковских карт.


## Функции содержащиеся в модуле: 

### `filter_by_currency` - Генератор, фильтрующий транзакции по заданному коду валюты.

Параметры:

* info_list — список транзакций (словарей).
* currency — строка с кодом валюты (например, "USD").

Возвращает: 
* Итератор с транзакциями, соответствующими указанной валюте.

Исключения:
* ValueError — если список пуст или транзакций с такой валютой нет.
* KeyError — если в транзакции отсутствует информация о валюте.

Пример использования:

```
from src.generators import filter_by_currency

transactions = [
    {"operationAmount": {"currency": {"code": "USD"}}, "amount": "100"},
    {"operationAmount": {"currency": {"code": "EUR"}}, "amount": "200"},
]

for txn in filter_by_currency(transactions, "USD"):
    print(txn) 
```
### `card_number_generator` - Генератор номеров банковских карт в 16-значном формате XXXX XXXX XXXX XXXX.

Параметры:

* start — начальное значение (включительно).
* stop — конечное значение (включительно).

Возвращает:

* Итератор с отформатированными номерами карт.

Исключения:

* ValueError — если start > stop.

Пример использования:

```
from src.generators import card_number_generator

for card in card_number_generator(1, 3):
    print(card)
```
## Модуль `csv_xlsx_utils` используется для работы с фалами .csv .xlsx

### В модуле реализованы две функции:
1. `get_csv` - функция которая принимает путь до csv-файла 
и возвращает список словарей с данными о финансовых транзакциях. 
Если файл пустой, не найден или путь некорректен, то функция возвращает пустой список.

2. `get_excel` - функция которая принимает путь до xlsx-файла 
и возвращает список словарей с данными о финансовых транзакциях. 
Если файл пустой, не найден или путь некорректен, то функция возвращает пустой список.

## Модуль `decorators` используется для размещения декораторов

### добавлен декоратор log который автоматически логирует начало и конец выполнения функции, а также ее результаты или возникшие ошибки.

Логирование включает:

* Имя функции и результат выполнения при успешной операции.
* Имя функции, тип возникшей ошибки и входные параметры, если выполнение функции привело к ошибке.

## **main** Финальный модуль проекта  
В нём реализована функция **main**, отвечающая за основную логику проекта и связывающая функциональности написанных ранее функций между собой.


## Тестирование:
* В папке tests проводятся тестирования функций с различными сценариями и подставлением данных
* Для проведения тестрирования необходимо ввести команду poetry run pytest --cov
 


### **Контакт для обратной связи**
 hdd220w@mail.ru
 